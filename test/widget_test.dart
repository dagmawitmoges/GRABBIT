import 'package:flutter_test/flutter_test.dart';
import 'package:grabbit/main.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Just verifies the app builds without crashing
    expect(true, isTrue);
  });
}